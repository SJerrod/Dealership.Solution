@using Dealership.Models;

<h1>Cars for Sale</h1>

@if (Model.Count == 0)
{
  <p>We Currently have no cars availible.</p>
}
<ul>
  @foreach (Car car in Model)
  {
    <li>@car.MakeModel</li>
    <li>@car.Price</li>
    <li>@car.Miles</li>
  }
</ul>

<a href="/cars/new">Add another Car.</a>